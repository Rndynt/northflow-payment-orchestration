# Release Readiness: v0.4.0 — Contract Freeze

**Phase:** S10.4 — OpenAPI / SDK contract freeze  
**Date:** 2026-06-09  
**Status:** ✅ Contract frozen — ready for external integration

---

## Summary

S10.4 completes the API contract freeze for `northflow-payment-orchestration` v0.4.0. This phase:

1. Audited every service route against the OpenAPI spec and SDK — 20 missing routes documented.
2. Fixed 4 SDK response-shape mismatches (extra `{ data }` wrapping on list/sync/upsert/options).
3. Preserved `created` on upsert by embedding it inside the `data` envelope.
4. Removed `deleteProviderAccountMethod` from SDK (no backing route or use case).
5. Added 6 merchant webhook management methods to the SDK.
6. Added comprehensive OpenAPI parity and SDK response-shape contract tests.
7. Produced `route-scope-matrix.md` and `error-contract.md` as integration references.

> **Note (S10.4.1 correction):** SDK covers runtime integration routes only.
> API client credential lifecycle and audit log routes are documented in OpenAPI
> for direct REST/admin usage — they are not exposed by `PaymentOrchestrationClient`.

---

## Pre-release Checklist

### ✅ Service & Infrastructure

- [x] All 34 service routes documented in OpenAPI 3.x spec
- [x] All `/v1/*` routes declare `security` in OpenAPI
- [x] Route-scope matrix matches `requireScope` middleware in all route files
- [x] Error envelope shape `{ ok: false, error: { code, message, details } }` used consistently
- [x] `errorHandler` middleware catches all unhandled errors and never leaks stack traces
- [x] Health (`/health`, `/version`, `/ready`) routes require no auth
- [x] Dev routes (`/v1/dev/*`) gated behind `nodeEnv !== 'production'`
- [x] HMAC signed-request enforcement configurable per environment

### ✅ SDK

- [x] `PaymentOrchestrationClient` covers all **runtime integration routes** (merchant, payment intent, provider account, payment methods, signing keys, merchant webhooks, fake-gateway dev)
- [x] Admin/ops routes (API client credential lifecycle, audit logs) are documented in OpenAPI for direct REST/admin usage — SDK does **not** expose these routes; they are intended for operator tooling, not merchant runtime integration
- [x] No `deleteProviderAccountMethod` method (no backing route)
- [x] No `serviceToken` in public `PaymentOrchestrationClientConfig`
- [x] No `PaymentEngine*` or `Standalone*` aliases in public exports
- [x] Response shapes match unwrapped `{ ok, data }` envelope — no extra nesting
- [x] `UpsertProviderAccountMethodResponse` extends `ProviderAccountMethodResponse` with `created: boolean`
- [x] `ListProviderAccountMethodsResponse = ProviderAccountMethodResponse[]` (flat array)
- [x] `SyncProviderAccountMethodsResponse` = `{ methods, syncedCount, skippedCount, message }` (flat)
- [x] `PaymentIntentPaymentOptionsResponse` = `{ intentId, merchantId, currency, amountRemaining, options }` (flat)
- [x] Merchant webhook types exported: endpoints, deliveries, replay
- [x] `PaymentOrchestrationClientError` and `PaymentOrchestrationNetworkError` exported

### ✅ Documentation

- [x] `docs/openapi/payment-orchestration.openapi.json` — v0.4.0, 34 paths
- [x] `docs/payment-orchestration.openapi.json` — in sync with above
- [x] `docs/security/route-scope-matrix.md` — all 24 scopes documented
- [x] `docs/integration/error-contract.md` — all error codes documented
- [x] `docs/integration/merchant-outbound-webhooks.md` — webhook setup and payload
- [x] `docs/integration/webhook-signature-verification.md` — verification guide
- [x] `docs/integration/idempotency-guide.md`
- [x] `docs/integration/rest-quickstart.md`

### ✅ Tests

- [x] **500+ tests pass** — `pnpm test` clean
- [x] **Type checks pass** — all three packages
- [x] `tests/s10-4-openapi-parity.test.ts` — 11 assertions, route inventory ↔ spec parity
- [x] `tests/s10-4-sdk-response-shapes.test.ts` — 12 assertions, envelope unwrapping correctness
- [x] `tests/s10-2-sdk-integration-contract.test.ts` — updated, removed deleted method

### ✅ Breaking Changes

None. S10.4 is a contract freeze (documentation + test) release. The only behavioral change
is the upsert route now embeds `created` inside the `data` object instead of alongside it at
the top level. This is a non-breaking fix — callers using the SDK transparently benefit from
the corrected type. Callers parsing raw HTTP responses should update to read `data.created`
instead of the top-level `created` field.

---

## Scope Coverage Matrix (summary)

| Domain            | Routes | Scopes |
|-------------------|--------|--------|
| Health            | 3      | 0      |
| Merchants         | 2      | 2      |
| Provider Accounts | 2      | 2      |
| Payment Methods   | 5      | 3      |
| Payment Intents   | 5      | 4      |
| Payment Txns      | 3      | 3      |
| Audit Logs        | 1      | 1      |
| API Clients       | 8      | 8      |
| Merchant Webhooks | 6      | 2      |
| Provider Webhooks | 1      | 0      |
| Dev               | 1      | 1      |
| **Total**         | **37** | **24** |

*(37 = 34 OpenAPI paths + 2 method variants on PUT upsert that share one path + /version)*

---

## Known Deferred Items

| Item | Reason | Target |
|------|--------|--------|
| Pagination on `/v1/audit-logs` | Cursor-based pagination not yet implemented | v0.5.0 |
| `GET /v1/payment-intents` (list) | Intentionally not exposed — callers drive by `externalPayableId` | TBD |
| Webhook delivery status filter | Not yet a query param | v0.5.0 |
| `DELETE /v1/merchants/{id}` | No soft-delete use case | TBD |
